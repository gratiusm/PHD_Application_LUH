# PHD_Application_LUH
This repository implements Bayesian Optimization (BO) with Weighted Expected Improvement (WEI) to find the optimal learning rate for a ResNet-18 trained on Fashion-MNIST through minimising the loss for an PHD Application at the LUH
